#include<stdio.h>
#include<conio.h>
int main()
{
    int l,b,h,volume;
    printf("value of l,b,h");
    scanf("%d %d %d",&l,&b,&h);
    volume=l*b*h;
    printf("volume of a cuboid is %d",volume);
    getch();

}