#include<stdio.h>
int main()
{
    float a,b,c;
    printf("Enter Two Number");
    scanf("%f %f", &a,&b);
    c = a*b;
    printf("Square Of Two Number is %f and %f are %f", a,b,c);
    return 0;

}